name: Test Running

permissions:
  contents: read
  actions: write # fount-charCI cache save/delete

on:
  workflow_dispatch:
  push:
    paths:
      - '**.mjs'
      - 'fount.json'
      - '.github/workflows/CI.yml'
    tags-ignore:
      - '*'
    branches:
      - '*'

jobs:
  test-running:
    runs-on: ubuntu-latest
    steps:
      - uses: steve02081504/fount-charCI@580c31ea82dacac99b67f3a97a39f8f769b52f3a
        with:
          CI-filepath: .github/workflows/CI.mjs
